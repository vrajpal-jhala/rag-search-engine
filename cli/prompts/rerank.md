Rank how well these movies match the search query.

Query: "{query}"

Movies:
{movies}

Return ONLY the IDs in order of relevance (best match first) as a JSON array without any formatting.

Example:
[32, 13, 45, 21, 10]
