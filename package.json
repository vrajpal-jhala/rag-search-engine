{
  "name": "rag-search-engine",
  "version": "1.0.0",
  "description": "Foundation project for building a RAG pipeline in node.js",
  "module": "cli/index.ts",
  "type": "module",
  "private": false,
  "scripts": {
    "start": "bun run cli/index.ts",
    "build": "echo 'Note: Compiled binary does not support cross-encoder re-ranking due to native dependencies' && bun build cli/index.ts --compile --outfile dist/rag-search-engine"
  },
  "devDependencies": {
    "@types/bun": "1.3.8"
  },
  "peerDependencies": {
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@google/genai": "1.41.0",
    "@huggingface/transformers": "3.8.1",
    "commander": "14.0.3",
    "natural": "8.1.0"
  }
}
